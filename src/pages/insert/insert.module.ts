import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { Insert } from './insert';

@NgModule({
  declarations: [
    Insert,
  ],
  imports: [
    IonicPageModule.forChild(Insert),
  ],
})
export class InsertModule {}
